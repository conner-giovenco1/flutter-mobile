import 'package:flutter/material.dart';

import 'main_wrapper.dart';

void main() => runApp(
      MaterialApp(
        debugShowCheckedModeBanner: false,
        home: const MyApp(),
      ),
    );
